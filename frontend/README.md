# FINDS Frontend

## 🚀 Запуск проекта

### 1. Запуск бэкенда (Spring Boot)
```bash
cd /Users/ilanevejkin/Desktop/FINDS
./mvnw spring-boot:run
```
Или через IDE:
- Откройте `DevApplication.java`
- Запустите основной метод

### 2. Запуск фронта (Python)
```bash
cd /Users/ilanevejkin/Desktop/FINDS/frontend
python3 server.py
```

### 3. Доступ к приложению
- **Фронт:** http://localhost:3001
- **Бэкенд:** http://localhost:8090
- **API тест:** http://localhost:3001/test-backend.html

## 📡 API Эндпоинты

### Аутентификация
- `POST /api/auth/signin` - вход
- `POST /api/auth/signup` - регистрация
- `POST /api/auth/refresh` - обновление токена

### Личный кабинет
- `GET /api/lk/me/get` - получить профиль
- `PATCH /api/lk/me/email` - изменить email
- `PATCH /api/lk/me/name` - изменить имя
- `PATCH /api/lk/me/password` - изменить пароль

## 🧪 Тестирование

### Способ 1: Через браузер
1. Откройте http://localhost:3001
2. Зарегистрируйтесь или войдите
3. Попадете в личный кабинет

### Способ 2: API тест
1. Откройте http://localhost:3001/test-backend.html
2. Тестируйте все эндпоинты напрямую

### Способ 3: Curl команды
```bash
# Регистрация
curl -X POST http://localhost:8090/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"name":"Test User","email":"test@example.com","password":"123456","confirmPassword":"123456","role":"USER"}'

# Вход
curl -X POST http://localhost:8090/api/auth/signin \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"123456"}'
```

## 🔧 Структура проекта

```
frontend/
├── index.html          # Главная страница (вход/регистрация)
├── dashboard.html      # Личный кабинет
├── config.js           # Конфигурация API и утилиты
├── script.js           # Логика аутентификации
├── dashboard.js        # Логика личного кабинета
├── server.py           # Python сервер для фронта
├── test-backend.html   # Тест API
└── styles.css          # Стили
```

## 🐛 Troubleshooting

### Проблема: "Load failed" / CORS ошибка
**Решение:** Убедитесь что бэкенд запущен и перезапустите его после изменений.

### Проблема: JWT ключ слишком короткий
**Решение:** Используется длинный ключ в `JwtCore.java`

### Проблема: Бэкенд не отвечает
**Решение:** Проверьте порт 8090:
```bash
lsof -ti:8090
```

### Проблема: Фронт не запускается
**Решение:** Используйте другой порт:
```bash
PORT=3002 python3 server.py
```

## 📝 Что было реализовано

✅ **Аутентификация:**
- Регистрация с валидацией
- Вход с JWT токенами
- Автообновление токенов

✅ **Личный кабинет:**
- Просмотр профиля
- Редактирование email, имени, пароля
- Защищенные эндпоинты

✅ **Безопасность:**
- Spring Security конфигурация
- JWT фильтрация
- CORS настройки

✅ **Фронт:**
- Современный интерфейс
- Валидация форм
- Обработка ошибок
- Адаптивный дизайн
